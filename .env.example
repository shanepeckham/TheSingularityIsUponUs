# Release Flow Environment Configuration
# Copy this file to .env and fill in your values

# GitHub Personal Access Token (required)
# Create at: https://github.com/settings/tokens
# Required scopes: repo, workflow
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx

# Target Repository (required)
# The repository to run release flow against
GITHUB_REPO_OWNER=your-username-or-org
GITHUB_REPO_NAME=your-repo-name

# Prompts Configuration
# Create a prompts.txt file with your prompts (one per line)
# The CLI will automatically use prompts.txt if it exists
# Or specify a custom file with: --prompts-file custom.txt

# Optional: If using Copilot CLI fallback
# COPILOT_CLI_PATH=/usr/local/bin/copilot
